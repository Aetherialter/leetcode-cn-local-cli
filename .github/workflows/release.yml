name: Publish release

on:
  push:
    tags:
      - "v*"

jobs:
  verify:
    name: Verify on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.28"
          python-version: "3.12"
      - name: Install development environment
        run: uv sync --locked --all-groups
      - name: Check formatting
        run: uv run ruff format --check src tests scripts
      - name: Run lint
        run: uv run ruff check src tests scripts pyproject.toml
      - name: Run type checking
        run: uv run pyright src tests scripts
      - name: Run tests
        run: uv run pytest
      - name: Build distributions
        run: uv build --no-sources
      - name: Verify Unix installer
        if: runner.os != 'Windows'
        shell: bash
        run: |
          wheel_path="$(find "$PWD/dist" -maxdepth 1 -type f -name '*.whl' -print -quit)"
          test -n "$wheel_path"
          export LEETCODE_LOCAL_CLI_INSTALL_SPEC="$wheel_path"
          export LEETCODE_LOCAL_CLI_NO_MODIFY_PATH=1
          export UV_TOOL_DIR="$RUNNER_TEMP/leetcode-local-cli-tools"
          export UV_TOOL_BIN_DIR="$RUNNER_TEMP/leetcode-local-cli-bin"
          ./scripts/install.sh
          "$UV_TOOL_BIN_DIR/lc" --help
      - name: Verify Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:LEETCODE_LOCAL_CLI_INSTALL_SPEC = (Resolve-Path "dist/*.whl").Path
          $env:LEETCODE_LOCAL_CLI_NO_MODIFY_PATH = "1"
          $env:UV_TOOL_DIR = Join-Path $env:RUNNER_TEMP "leetcode-local-cli-tools"
          $env:UV_TOOL_BIN_DIR = Join-Path $env:RUNNER_TEMP "leetcode-local-cli-bin"
          & ./scripts/install.ps1
          & (Join-Path $env:UV_TOOL_BIN_DIR "lc.exe") --help

  publish:
    name: Publish to PyPI
    needs: verify
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/leetcode-local-cli
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.28"
          python-version: "3.12"
      - name: Verify release tag
        shell: bash
        run: test "v$(uv version --short)" = "$GITHUB_REF_NAME"
      - name: Build distributions
        run: uv build --no-sources
      - name: Smoke test wheel
        shell: bash
        run: |
          export LEETCODE_LOCAL_CLI_EXPECTED_VERSION="$(uv version --short)"
          uv run --isolated --no-project --with dist/*.whl scripts/smoke_test.py
      - name: Smoke test source distribution
        shell: bash
        run: |
          export LEETCODE_LOCAL_CLI_EXPECTED_VERSION="$(uv version --short)"
          uv run --isolated --no-project --with dist/*.tar.gz scripts/smoke_test.py
      - name: Publish distributions
        run: uv publish --trusted-publishing always

  release:
    name: Create GitHub release
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "GitHub release $GITHUB_REF_NAME already exists"
          else
            gh release create "$GITHUB_REF_NAME" \
              --repo "$GITHUB_REPOSITORY" \
              --verify-tag \
              --title "$GITHUB_REF_NAME" \
              --generate-notes
          fi
