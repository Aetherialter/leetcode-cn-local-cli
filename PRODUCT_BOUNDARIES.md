# 产品边界与待定决策

最近更新：2026-07-26

状态：持续维护。本文记录当前已经确认的产品边界，以及必须由项目维护者拍板后才能进入实现的行为语义。当前实现、README、路线图和长期设计如有冲突，以“已确认边界”为当前版本依据；“待定决策”不得被实现建议代替。

## 已确认边界

| ID | 主题 | 已确认结论 | 实现状态 |
| --- | --- | --- | --- |
| PB-C01 | 当前工作流 | v0.7.x 保持单题、单文件工作流，CLI 启动目录中的 `solution.py` 是唯一主要工作文件。 | 已实现 |
| PB-C02 | 普通文件覆盖 | `lc solve <题号>` 表示切换当前题目，可以无确认覆盖普通 `solution.py`。 | 已实现 |
| PB-C03 | 非普通写入目标 | 符号链接、断链、目录、目录链接和 Windows reparse point 不属于普通文件，必须拒绝，不能跟随写入。 | 待修复 |
| PB-C04 | 自动打开 | 当前 `solve` 成功后继续使用系统默认关联打开 `solution.py`；这是已接受的兼容性风险，后续由明确编辑器配置替代或覆盖。 | 当前行为已实现 |
| PB-C05 | Doctor 执行边界 | `lc doctor` 默认不执行工作区代码；只有显式 `--run-solution` 才运行 `solution.py`。 | 已实现 |
| PB-C06 | uv 安装边界 | 安装器未检测到 uv 时只提示 uv 官方文档，不自动下载或执行远端安装脚本。 | 已实现 |
| PB-C07 | 当前数据范围 | 当前不保存完整题面、不引入数据库、不生成每题独立目录。 | 已实现 |

## 待定决策

### PB-001：`lc test` 的执行契约

当前实现执行整个 `solution.py`，由文件中的 `if __name__ == "__main__"` 自行调用 `run_cases()`；CLI 不验证或主动调用该函数。因此，语法合法但没有 `run_cases()` 的非空文件也可能显示测试通过。

待决定：

1. CLI 必须验证并主动调用 `run_cases()`。
2. CLI 只执行整个文件，`run_cases()` 仅作为模板惯例。
3. 文件必须定义 `run_cases()`，但仍由文件入口负责调用。

实现前必须同时定义如何避免模板入口和 CLI 重复执行 `run_cases()`。

### PB-002：本地测试输出与调试信息

待决定：

- 成功时是否展示用户程序的 stdout。
- 失败时是否展示 stdout、stderr 和简化异常。
- 是否新增 `lc test --verbose` 展示完整用户 traceback。
- `lc doctor --run-solution` 是否永久保持静默，只报告退出码或超时。

### PB-003：远程提交退出码

待决定：进程退出码表达“请求已成功发送”，还是表达“最终判题通过”。必须覆盖 `Accepted`、所有非 Accepted 状态、请求失败、认证失败和轮询超时。

建议候选语义：只有 `Accepted` 返回 0；非 Accepted 和运行失败返回非零；命令参数错误返回 2。

### PB-004：提交前本地校验

待决定：`lc submit` 在远程请求前是否编译整个 `solution.py`、提交 marker 区域，或完全交给 LeetCode 判定。该决定不得让 `lc submit` 隐式执行用户代码。

### PB-005：工作区写入事务

待决定：

- 写入失败时是否保证旧 `solution.py` 字节级保持不变。
- 是否统一采用同目录随机临时文件和原子替换。
- 编辑器打开失败是否只警告并保持 `solve` 成功。
- 是否在网络请求前进行低成本可写性预检。

无论最终选择如何，PB-C03 的非普通目标拒绝规则不得放宽。

### PB-006：`solution.py` 编码

待决定：只接受 UTF-8，还是支持 Python 编码声明或自动猜测本地编码。当前生成文件为 UTF-8；非 UTF-8 文件会触发尚未收敛的读取异常。

### PB-007：编辑器配置模型

待决定：命令参数、工作区配置、用户配置、环境变量和系统默认关联之间的优先级，以及编辑器配置使用单个命令字符串还是参数数组。配置设计必须避免通过 Shell 拼接执行命令。

### PB-008：v0.8 工作区模型

当前 v0.7.x 的单文件行为已经确定，但 v0.8 及 v1.0 的正式工作区模型尚未确定。必须决定：

- `lc init` 只创建配置，还是改变文件布局。
- `solution.py` 位于命令启动目录还是工作区根目录。
- 是否允许从工作区子目录运行命令。
- 何时引入每题目录、自动归档或继续保持单文件。
- `lc init` 与 `lc solve` 对已有普通 `solution.py` 分别采用什么覆盖规则。

### PB-009：秘密存储不可用时的降级

待决定：Linux/WSL 没有 Secret Service 时，是登录失败、仅在当前进程使用，还是允许用户显式选择文件存储；不得静默降级为明文 Cookie。还需决定旧 Session 迁移成功后的保留或删除策略。

### PB-010：分页越界

待决定：合法但超过题目总数的 `show --skip` 返回空结果、友好提示并成功结束，还是作为用户错误返回非零。非法负数和远端结构异常必须与合法空页区分。

### PB-011：Broken Pipe

待决定：`lc --help | head` 等下游提前关闭管道的场景是否视为正常结束，以及是否统一返回 0。

### PB-012：Python 支持范围

待决定：继续声明无上限的 `Python >=3.12`，还是只支持 CI 实际覆盖的小版本范围；同时明确 uv 是否允许自动使用 managed Python。

## 决策规则

每项待定边界拍板后，应完成以下同步：

1. 将结论、日期和适用版本写入本文件，并从“待定决策”移动到“已确认边界”。
2. 更新 README 的用户可见行为。
3. 更新 ROADMAP 的实施版本与验收标准。
4. 如影响长期架构，更新 `PROJECT_DESIGN.md`。
5. 如影响安全或数据完整性，更新 `SECURITY_REVIEW.md`。
6. 实现代码与测试必须引用同一行为语义，不得只修改文档或只修改测试。

## 建议决策顺序

1. PB-001 `lc test` 执行契约。
2. PB-003 远程提交退出码。
3. PB-005 工作区写入事务。
4. PB-006 文件编码。
5. PB-002 测试输出与调试信息。
6. PB-004 提交前本地校验。
7. PB-008 v0.8 工作区模型。
8. PB-009 秘密存储降级。
9. PB-007 编辑器配置。
10. PB-010、PB-011、PB-012 的兼容性边界。
