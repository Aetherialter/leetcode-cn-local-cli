# Project Goal

`leetcode-local-cli` 的当前目标是提供一个面向 LeetCode 中文站的轻量、在线优先、本地刷题 CLI，完成登录、题目查询、单文件解题、本地测试、环境诊断和远程提交的可靠闭环。

项目的长期目标是在不引入强制数据库、Web、AI 或多语言提交的前提下，演进为同时支持 LeetCode 中文站与国际站的跨平台工具，并提供共享同一核心实现的稳定 CLI 和 Python API。

开发必须尊重当前产品边界：

- 当前已发布版本是 `v0.9.0`；当前工作树在此基础上完成了 CLI 分层重构，但尚未发布新版本。
- 当前使用用户配置指向的单个默认工作区和单个 `solution.py`，`lc solve` 可以原子覆盖普通文件。
- 符号链接、断链、目录、目录链接和 Windows reparse point 不是允许覆盖的普通目标。
- 当前只正式支持 Python3 提交，不引入本地数据库或完整题库。
- 用户配置与工作区路径已经分离；Cookie 当前阶段性保存在默认工作区，但长期仍应迁移到系统秘密存储。
- 长期设计仍在分阶段实施。不得把设计草案中的 Python API、双站点、图片或 UI 能力描述为当前已实现功能。

项目状态与行为发生冲突时，按以下顺序判断：

1. `docs/PRODUCT_BOUNDARIES.md` 中已经确认的产品边界。
2. `docs/PROJECT_STATUS.md` 记录的当前实现状态。
3. `docs/ARCHITECTURE.md` 记录的真实架构和依赖。
4. `docs/DEVELOPMENT_PLAN.md` 记录的实施顺序。
5. `docs/PROJECT_DESIGN.md` 中尚未落地的长期设计方向。

# Development Philosophy

- 按成熟开源项目标准完成设计、实现、测试、文档、Review 和发布准备，不以“能够运行”代替完整交付。
- 优先维护性、正确性、安全性和清晰边界，而不是最快写出功能。
- 采用满足需求的最小可维护改动，避免临时代码、重复实现、固定成功返回、隐藏占位和未说明的 `TODO`。
- 保持架构稳定。新功能应进入职责正确的模块，不通过在 `cli.py`、命令适配器或 UI 中堆叠业务条件绕开现有边界。
- 保持 `cli → commands → use_cases → core modules` 的单向依赖。禁止底层模块反向导入 `use_cases`、`commands`、`cli` 或 `ui`。
- 核心能力不依赖 Typer、Rich 或具体终端；CLI 和未来 Python API 应复用同一应用逻辑。
- 路径必须由显式运行上下文或函数参数提供。不得新增导入时捕获 `Path.cwd()` 的全局状态。
- 站点差异应显式封装，不在多个模块中散布域名和站点判断。
- 公开成功结果优先使用明确、不可变的类型模型；不得扩散新的裸 `dict`、`Any` 或传输层响应作为稳定 API。
- 业务错误使用结构化结果或项目异常表达，中文文案和退出码由 CLI 映射。
- 外部接口、用户输入、配置和工作区内容均是不可信数据。不得把它们解释为 Rich markup、终端控制序列、Shell 命令或隐式链接。
- 同步优先。没有明确并发需求时，不引入 async/sync 双接口或额外框架。
- 不以“生产级”为理由擅自扩大范围。数据库、缓存、AI、Web、后台同步、多语言和工作区模型变化必须经过明确的产品决策。
- 保留用户已有工作和兼容行为。禁止用大规模重写掩盖局部问题，禁止无关重构、批量格式化或依赖升级。

# Before Coding

任何功能、修复或架构改动开始前，必须先完成以下步骤。

## 1. 阅读当前上下文

必须完整阅读与当前任务相关的内容：

- `docs/PROJECT_STATUS.md`
- `docs/ARCHITECTURE.md`
- `docs/DEVELOPMENT_PLAN.md`

还必须按任务范围检查：

- `docs/TECH_DECISIONS.md`：已有技术选择、替代方案和开放决策。
- `docs/PRODUCT_BOUNDARIES.md`：用户可见行为是否已经确认。
- `docs/SECURITY_REVIEW.md`：任务是否涉及秘密、文件写入、执行、终端或远端接口风险。
- `README.md`、相关源码、测试、配置和 CI：文档描述是否与真实实现一致。

不得只根据文件名或历史计划推断现状。

## 2. 分析影响

开始编码前必须说明：

- 功能解决什么真实问题，成功标准是什么。
- 功能应属于哪个模块，为什么由该模块负责。
- 在当前分层中明确它属于 CLI 注册、命令适配、应用用例还是底层能力；不得创建与 `commands/` 或 `use_cases/` 职责重复的平行层。
- 是否改变现有模块职责或依赖方向。
- 输入、输出、错误和副作用的数据流如何变化。
- 是否影响 CLI 参数、退出码、公开模型、未来 Python API 或兼容行为。
- 是否涉及文件系统、凭据、用户代码执行、网络请求、跨平台或发布风险。
- 需要新增或调整哪些单元、集成、CLI、跨平台或手动验收测试。

如果实现会碰到 `docs/PRODUCT_BOUNDARIES.md` 中的待定项，必须先完成产品决策，不能让当前实现偶然冻结行为。

## 3. 提出方案

实施前必须给出：

- 推荐方案和适用边界。
- 选择该方案的原因。
- 主要优点与缺点。
- 至少一个经过认真评估的替代方案。
- 兼容性、安全、维护成本和迁移方面的技术取舍。
- 可以验证方案完成的验收标准。

小型、局部、可逆且不改变公开行为的改动，在完成上述简要分析后可以直接推进。未经用户确认，不得直接进行大规模重构、跨模块迁移、公开 API 破坏、工作区模型变化、凭据迁移或其他会显著改变架构和用户行为的修改。

# Coding Rules

- `cli.py` 只负责创建 Typer 应用、全局回调和注册命令；不得重新放入具体命令实现。
- `commands/` 只负责 `lc ***` 的参数声明、终端输入、Rich 渲染、错误文案和退出码映射。可复用流程必须委托给 `use_cases/` 或职责明确的底层模块。
- `use_cases/` 负责跨模块业务流程和失败边界，不得导入 Typer、Rich、`commands`、`cli` 或 `ui`。需要进度或阶段性输出时，只接收窄回调或协议，由命令层注入实现。
- `auth.py`、`browser.py`、`client.py`、`workspace.py` 等当前继续作为扁平的底层能力模块；只有出现真实子域和多个内聚模块时才拆包，不为目录对称而搬迁。
- 用例错误当前使用 `UseCaseError` 映射到 CLI；新增稳定错误语义时应增加结构化类别，不允许调用方解析中文错误文案。
- 遵循单一职责。函数、类和模块应只有清晰的变化原因。
- 使用表达业务含义的清晰命名，避免含糊缩写、临时编号和与远端字段混淆的名称。
- 保持模块低耦合和单向依赖。领域模型、认证、工作区和 HTTP 逻辑不得反向依赖应用用例、CLI 或 UI。
- 避免重复代码。重复业务规则应收敛到职责正确的内部入口，但不得为消除少量重复引入不必要抽象。
- 保持 API 稳定。删除、重命名或改变公开命令、函数、模型字段、异常和退出码前，必须分析兼容性并提供迁移策略。
- 使用类型注解；面向公共消费者的返回值不得新增无约束的 `Any` 或裸字典。
- 数据模型优先使用冻结 dataclass 或项目已经采用的等价不可变结构，不为简单模型引入重量级依赖。
- 网络请求必须设置明确超时，验证 HTTP、JSON 和响应结构；只对安全可重试操作进行有限重试。
- 子进程使用参数数组，不使用 `shell=True`，不拼接来自用户或配置的命令。
- 文件写入必须考虑符号链接、reparse point、权限、占用、编码、部分写入和原子替换；不得在失败时静默破坏用户文件。
- 凭据不得硬编码、提交、记录或通过异常输出。测试只能使用明确的假 Cookie 和脱敏 fixture。
- 外部字符串只能作为纯文本渲染；样式和可点击链接必须由本地可信代码显式创建。
- 保持跨平台行为。涉及路径、权限、文件锁、子进程、浏览器或编辑器时，必须分别考虑 Windows、macOS 和 Linux。
- 不提交生成缓存、构建产物、真实 `solution.py` 解法、Session、日志、秘密或本机配置。
- 不修改、重置、暂存或提交与当前任务无关的用户改动。

# Testing

任何新功能或行为修复都必须添加或更新测试，并覆盖与风险相称的成功路径、失败路径和边界情况。

至少检查：

- 核心逻辑和数据模型。
- 非法输入、空值、类型错误和边界值。
- 网络、HTTP、无效 JSON、接口结构变化、认证失败和超时。
- 文件不存在、目录、链接、权限、占用、编码和写入中断。
- 进程退出码、超时、异常传播和资源释放。
- 来自远端或本地的不可信终端文本。
- Windows、macOS 和 Linux 的重要行为差异。

测试原则：

- 使用 `tmp_path`、`monkeypatch`、`httpx.MockTransport` 和 Typer `CliRunner` 隔离副作用。
- 命令行为测试放在 `tests/test_cli.py`；用例行为和依赖边界测试放在 `tests/test_use_cases.py`；底层模块测试继续按对应模块命名。
- 用例层必须保持无 Typer、Rich 和 `ui.py` 依赖；任何新增反向依赖都必须由自动测试阻止。
- CI 不保存真实 LeetCode Cookie，不执行真实远程提交。
- Mock、stub 和 fake 只用于隔离依赖，不能替代被请求的核心业务实现。
- 不删除、跳过或弱化现有测试来获得通过。
- 失败必须先判断是本次改动引入还是已有问题；没有修改前证据时，不得声称失败是预先存在的。

代码变更完成前，按任务范围运行：

```shell
uv run ruff format --check src tests scripts
uv run ruff check src tests scripts pyproject.toml
uv run pyright src tests scripts
uv run pytest
```

影响构建、入口、安装或公开包内容时，还必须运行：

```shell
uv build --no-sources
uv run lc --version
uv run lc --help
```

影响真实站点、浏览器、操作系统或凭据的行为如果无法自动验证，必须记录未验证部分、替代检查、剩余风险和所需手动验收环境。

# Documentation

功能完成后必须同步维护长期上下文：

- 更新 `docs/PROJECT_STATUS.md`：记录新增能力、当前阶段、开放问题、验证结果和下一步。
- 如果模块职责、依赖方向、数据流、运行上下文或公开边界发生变化，更新 `docs/ARCHITECTURE.md`。
- 更新 `docs/DEVELOPMENT_PLAN.md`：标记阶段进展、调整优先级和后续计划。
- 如果做出或替代了重要技术选择，更新 `docs/TECH_DECISIONS.md`。

根据改动范围，还应同步：

- 用户可见命令、行为、限制或安装方式变化：更新 `README.md`。
- 产品语义被确认或改变：更新 `docs/PRODUCT_BOUNDARIES.md`。
- 安全风险、缓解措施或剩余限制变化：更新 `docs/SECURITY_REVIEW.md`。
- 长期架构或 v1.0 范围变化：更新 `docs/PROJECT_DESIGN.md`。
- 发布版本：更新版本号、锁文件和 `docs/release-notes/` 中对应版本的 Release Notes。

文档必须描述真实实现和真实验证结果，不得把计划、mock 验证或未运行的检查写成已完成。

# Mentor Mode

本项目采用 Pair Programming + Mentor 模式。AI 的目标是帮助开发者形成独立设计、实现、测试和维护开源项目的能力，而不是代替开发者批量生成代码。

每个功能按以下节奏进行：

1. **需求分析**：解释功能解决的问题、用户价值、边界和成熟开源项目中的常见做法。
2. **技术设计**：解释涉及模块、数据流、依赖、失败路径、推荐方案和替代方案。
3. **教学实现**：给出必要的核心示例和文件位置，解释关键思想，让开发者完成主体实现。
4. **实现 Review**：检查正确性、架构、命名、维护性、性能和安全；发现问题时先说明原因和影响，让开发者尝试修正。
5. **验证与总结**：说明运行了哪些检查、学到了什么、项目新增了什么能力、做了哪些取舍以及下一步方向。

讲解必须同时包含：

- **Why**：为什么需要这个设计，不采用时会出现什么问题。
- **How**：数据和控制如何流动，代码如何放入当前架构。
- **Trade-off**：方案获得了什么、付出了什么，以及何时应选择替代方案。

遇到依赖注入、适配器、不可变模型、错误边界、原子写入、超时、幂等性等概念时，必须先解释其解决的问题和适用条件，再讨论代码写法。不要假设开发者已经理解术语，也不要用玩具示例替代项目中的真实工程约束。

所有面向用户的交流默认使用中文；代码、标识符、注释、文档和 Git commit message 遵循仓库既有语言和风格。
