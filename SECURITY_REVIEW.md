# 安全审计与修复清单

审计日期：2026-07-20

最近更新：2026-07-24

审计范围：`src/`、安装脚本、发布工作流和当前运行时依赖

状态：以下问题为源码审计结果，各项当前状态见风险清单

## 总体结论

当前项目没有监听网络端口，也未发现无需用户操作即可通过远程请求直接控制程序的漏洞。主要攻击面来自本地工作区、明文登录凭据、自动执行 Python 文件、终端渲染和安装供应链。

审计时最现实的攻击路径是诱导用户进入不可信目录并执行 `lc doctor`，使其中的 `solution.py` 获得当前用户权限；该路径已于 2026-07-24 修复，默认诊断不再执行工作区代码。

## 风险清单

| ID | 等级 | 问题 | 主要影响 | 状态 |
| --- | --- | --- | --- | --- |
| SR-001 | 高 | `lc doctor` 自动执行工作区代码 | 任意代码以当前用户权限执行 | 已修复 |
| SR-002 | 高 | Cookie 明文保存在当前目录 | Session 泄漏、误提交或同步 | 待修复 |
| SR-003 | 中 | 系统关联自动打开 `.py` | Windows 上可能直接执行生成文件 | 待修复 |
| SR-004 | 中 | 文件写入未防御符号链接 | 覆盖用户权限范围内的其他文件 | 待修复 |
| SR-005 | 中 | Rich 渲染未转义外部文本 | 终端超链接或显示内容注入 | 已修复 |
| SR-006 | 中 | uv 引导脚本缺少内容校验 | 上游供应链异常时执行非预期代码 | 待修复 |
| SR-007 | 低 | Cookie 域名边界匹配过宽 | 接受名称以 `leetcode.cn` 结尾的非目标域名 | 已修复 |
| SR-008 | 低 | 部分响应边界依赖 `assert` | 优化模式下可能退化为异常或拒绝服务 | 待修复 |

## 详细发现

### SR-001：Doctor 执行不可信代码

原实现的 `doctor.diagnose_solution()` 对 `READY` 和 `NOT_SUBMITTABLE` 状态都会调用 `run_solution_file()`。这意味着当前目录中任何语法有效的 `solution.py`，即使没有 LeetCode marker，也会在执行 `lc doctor` 时运行。

攻击前提：用户在不可信目录执行 `lc doctor`。

影响：文件和凭据读取、网络访问、数据破坏，以及当前用户权限范围内的任意操作。10 秒超时只能终止长时间运行，不能撤销已经产生的副作用。

修复要求：

- `lc doctor` 默认只进行存在性、读取、语法和结构检查。
- 执行检查必须改为显式选项，例如 `lc doctor --run-solution`。
- 文档必须说明 `lc test` 和显式运行诊断会执行本地代码。

状态：已于 2026-07-24 修复。

修复内容：

- `lc doctor` 默认只检查本地文件，不再执行工作区代码。
- 新增显式 `lc doctor --run-solution` 选项，保留按需运行和超时诊断。
- README 明确 `lc test` 与显式运行诊断的代码执行边界。
- 回归测试覆盖默认不执行、显式执行和 CLI 参数传递。

### SR-002：Session Cookie 明文绑定当前目录

`auth.py` 在模块导入时使用 `Path.cwd()` 确定 `.leetcode_local_cli/session.json`，并将 `LEETCODE_SESSION` 与 `csrftoken` 写入普通 JSON 文件。

风险包括：

- 在其他 Git 仓库中登录后，凭据可能被意外提交。
- 工作目录位于同步盘或共享目录时，凭据可能被复制到其他设备或账号。
- Windows 上 `os.chmod(..., 0o600)` 不能替代可靠的仅当前用户可读 ACL。

修复要求：

- 凭据迁移到 Windows Credential Manager、macOS Keychain 或 Linux Secret Service。
- 用户配置目录和工作区目录必须分离。
- 秘密存储不可用时明确失败或仅在当前进程中使用，不得静默明文降级。
- 迁移和诊断过程不得输出 Cookie。

### SR-003：自动打开 Python 文件可能变成执行

`workspace.write_solution_file()` 写入远端返回的 Python 模板后立即调用 `open_path()`。Windows 实现使用 `os.startfile()`，行为等同于使用系统默认关联打开文件。如果 `.py` 关联到 Python 解释器，生成文件可能被直接执行。

修复要求：

- 默认只打印生成文件路径。
- 打开编辑器改为显式 `--open` 或经过配置的编辑器命令。
- 不使用 `.py` 的通用系统关联推断编辑器。

### SR-004：符号链接与临时文件覆盖

`solution.py` 通过普通写模式直接截断，Session 使用固定名称 `session.json.tmp` 并允许覆盖。恶意工作区可预先放置符号链接、目录链接或 Windows reparse point，使写入落到预期目录以外。

修复要求：

- 写入前拒绝符号链接和 reparse point。
- 使用同目录随机临时文件、排他创建和原子替换。
- 替换前重新核对解析后的父目录和文件类型。
- `lc solve` 覆盖现有文件前提供明确行为或确认机制。

### SR-005：Rich 终端标记注入

原实现将题目标题、标签、题面、账号字段、提交元数据和判题消息等外部数据以普通字符串交给启用 markup 的 Rich 组件。构造的 `[link=...]` 可生成 OSC 8 终端超链接并伪造显示目标。

状态：已于 2026-07-21 修复。

修复内容：

- 全局 Rich Console 关闭隐式 markup 与自动高亮。
- 外部字符串统一构造为纯文本 `Text`，样式只由本地固定代码显式指定。
- 外部文本进入终端前过滤 ANSI、OSC 等控制字符，同时保留换行和制表符。
- Profile、题目列表、题目详情、提交目标、Doctor 和判题结果均使用同一安全文本边界。
- 回归测试覆盖合法标记、畸形标记、ANSI、OSC 8 和无颜色输出。
- `PROJECT_DESIGN.md` 已将外部文本安全边界列为后续普通 CLI、TUI 和纯文本 UI 的共同约束。

### SR-006：安装引导供应链

安装器通过 HTTPS 下载 uv 官方安装脚本后直接执行，但没有固定内容摘要或签名。HTTPS 能保护传输过程，不能覆盖上游发布源或账号被攻破的情况。

修复建议：

- 尽可能固定经过审查的 uv 版本。
- 校验官方发布的 SHA-256 摘要或签名。
- 保留现有 HTTPS 与 TLS 限制。
- 在发布流程中记录安装器和构建产物摘要。

### SR-007：Cookie 域名匹配边界

原实现使用 `normalized_domain.endswith("leetcode.cn")`，会把 `evil-leetcode.cn` 视为符合条件。虽然浏览器 Cookie 查询还会提供一层过滤，但本地代码不应依赖第三方实现保证域名边界。

状态：已于 2026-07-22 修复。

修复内容：域名匹配改为大小写不敏感的严格边界判断，只接受 `leetcode.cn` 本身或以 `.leetcode.cn` 结尾的真实子域名，并增加根域、子域、伪造后缀域和后缀附加域的回归测试。

### SR-008：断言与异常边界

客户端在多个远端响应边界使用 `assert isinstance(...)`。使用 `python -O` 时断言会被移除，异常响应可能继续流入后续逻辑并产生 traceback。浏览器 Cookie 读取还会捕获所有 `Exception` 后静默继续，降低可诊断性。

修复建议：使用显式条件返回结构化错误，并只捕获已知的浏览器读取异常。

## 已有安全措施

- LeetCode 客户端基础地址固定为 HTTPS，未发现明显 SSRF 路径。
- 子进程使用参数列表，未使用 `shell=True`。
- GraphQL 使用变量传值，不拼接用户输入。
- 手动 Cookie 输入不回显。
- Doctor 和普通错误输出不包含 Cookie 值。
- `.leetcode_local_cli/` 与旧 `.aether_lc/` 已在当前仓库忽略。
- 当前 Git 历史未发现真实 Session 文件。
- GitHub Actions 的第三方 Action 固定到提交 SHA。
- PyPI 发布使用 OIDC Trusted Publisher，不保存 PyPI Token。

## 自动化检查结果

本次审计执行了以下辅助检查：

- `pip-audit`：当前解析环境未发现已知依赖漏洞。
- Bandit：报告 17 项低危提示，主要涉及子进程、断言和宽泛异常；人工审计发现了上述更高等级的业务逻辑风险。
- Ruff、Pyright 和现有 pytest 在此前修改验证中通过。

自动化扫描结果只代表其规则和漏洞数据库覆盖范围，不能替代业务逻辑审查。

## 修复优先级

### P0：立即处理

1. 将 Cookie 从工作区明文文件迁移到系统秘密存储。

### P1：下一版本阻断项

1. 移除 `.py` 系统关联自动打开。
2. 防御符号链接和非原子工作区写入。

### P2：持续加固

1. 移除生产代码中的边界断言和宽泛异常。
2. 为安装引导增加版本与摘要校验。
3. 在 CI 加入 `pip-audit`、Bandit 和秘密扫描。

## 与长期设计的关系

`PROJECT_DESIGN.md` 中的 v0.8 已计划引入显式运行上下文、用户配置目录和系统秘密存储，能够覆盖 SR-002 的核心问题。SR-001、SR-003 和 SR-004 应一并提升为 v0.8 或下一安全修复版本的阻断项，而不应等到 v1.0 发布收口阶段。SR-005 的安全边界已经写入长期设计，后续 UI 全面改造必须保持相同约束。
