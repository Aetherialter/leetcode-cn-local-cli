# 产品边界与待定决策

最近更新：2026-08-01

状态：持续维护。本文记录当前已经确认的产品边界，以及必须由项目维护者拍板后才能进入实现的行为语义。当前实现、README、路线图和长期设计如有冲突，以“已确认边界”为当前版本依据；“待定决策”不得被实现建议代替。

## 已确认边界

| ID | 主题 | 已确认结论 | 实现状态 |
| --- | --- | --- | --- |
| PB-C01 | 当前工作流 | v0.7.x 保持单题、单文件工作流，CLI 启动目录中的 `solution.py` 是唯一主要工作文件。 | 已实现 |
| PB-C02 | 普通文件覆盖 | `lc solve <题号>` 表示切换当前题目，可以无确认覆盖普通 `solution.py`。 | 已实现 |
| PB-C03 | 非普通写入目标 | 符号链接、断链、目录、目录链接和 Windows reparse point 不属于普通文件，必须拒绝，不能跟随写入。 | 已实现 |
| PB-C04 | 自动打开 | 当前 `solve` 成功后继续使用系统默认关联打开 `solution.py`；这是已接受的兼容性风险，后续由明确编辑器配置替代或覆盖。 | 当前行为已实现 |
| PB-C05 | Doctor 执行边界 | `lc doctor` 默认不执行工作区代码；只有显式 `--run-solution` 才运行 `solution.py`。 | 已实现 |
| PB-C06 | uv 安装边界 | 安装器未检测到 uv 时只提示 uv 官方文档，不自动下载或执行远端安装脚本。 | 已实现 |
| PB-C07 | 当前数据范围 | 当前不保存完整题面、不引入数据库、不生成每题独立目录。 | 已实现 |
| PB-C08 | v0.8 工作区 | 普通命令使用用户配置中的单个默认工作区；工作区根目录包含版本化标记和单个 `solution.py`，当前不提供全局 `--workspace`。 | 已实现 |
| PB-C09 | 初始化语义 | `lc init` 无路径时输入父目录并追加 `leetcode-local-cli`；显式路径是完整目标；初始化保留已有普通 `solution.py`，拒绝损坏配置和非普通目标。 | 已实现 |
| PB-C10 | v0.8 Session | Cookie 暂时保存在默认工作区 `.leetcode_local_cli/session.json`，不使用系统秘密存储、不迁移旧 Session；该文件不得输出或提交。 | 已实现，长期待替换 |
| PB-C11 | 写入事务 | `solution.py` 和 Session 使用同目录随机临时文件与原子替换；覆盖失败时旧文件保持不变，链接、目录和 reparse point 始终拒绝。 | 已实现 |
| PB-C12 | `lc test` 契约 | `lc test` 在独立 worker 中加载 `Solution`，按定义顺序选择第一个不以 `_` 开头的实例方法。交互输入采用 `name = value` 参数赋值，不输入预期结果；仅接受安全 Python 字面量（数字、字符串、列表、字典、元组、`True`、`False`、`None`），无参方法可输入 `()`，链表和二叉树暂不自动转换。每组调用使用新的 `Solution` 实例，默认各自限时 1 秒；超时终止 worker，下一组重启。输入错误、异常或超时以受控错误展示但不终止交互，最终只要任一组失败即返回 1；所有已输入调用正常完成才返回 0，该结果不代表算法已验证正确。连续两次空输入退出。`--stdin` 读取相同的逐行输入并输出 JSON Lines（含最终 summary），不使用 Rich 表格或创建 cases 文件。`lc submit` 不执行本地调用。 | v0.9 已实现 |
| PB-C13 | `solution.py` 编码 | 只接受 UTF-8，并兼容 UTF-8 BOM；不猜测、不替换非法字节、不自动转换或重写用户文件。编码失败在 `test`、`doctor`、`submit` 中返回受控非零结果，不执行文件且不发送远端提交请求。 | v0.9 已实现 |
| PB-C14 | 远程提交退出码 | `lc submit` 的退出码表达最终判题结果，而不是仅表达请求已发送。只有明确的 `Accepted` 返回 0；所有非 Accepted、轮询超时、无法识别的终态、网络、认证和响应错误返回 1；命令使用错误返回 2。终端无论成功失败都会正常展示结果并返回提示符。 | v0.9 已实现 |
| PB-C15 | 浏览器 DevTools 登录 | 普通 `lc login` 依次尝试用户在 `chrome://inspect/#remote-debugging`、`edge://inspect/#remote-debugging` 明确勾选允许调试的当前日常 Chrome、Edge 和隐藏的手动 Cookie。`--browser chrome|edge` 固定单一浏览器，失败后不切换另一浏览器；`--devtools-port` 必须与明确浏览器组合。Chrome 和 Edge 都只读各自默认用户目录中的 `DevToolsActivePort`，通过 approval-only 浏览器 WebSocket 连接，不创建专用配置、不读取 Cookie 数据库、不关闭日常实例，也不绕过浏览器要求的两步用户授权。浏览器未运行、授权文件指向旧实例或后台浏览器以 403 拒绝连接时，只打开一次可见窗口，并在 180 秒总时限内有限重试临时连接错误；身份、协议等确定性错误立即失败。所有 DevTools 端点必须是已验证身份的本机回环端点，只请求 `https://leetcode.cn/` 适用 Cookie 并保留两个必需字段。网络或 Session 写入失败不得通过浏览器回退掩盖。 | v0.9 已实现 |

## 待定决策

### PB-002：扩展调试输出与 Doctor 执行信息

待决定：

- 是否新增 `lc test --verbose` 展示完整用户 traceback。
- `lc doctor --run-solution` 是否永久保持静默，只报告退出码或超时。

### PB-004：提交前本地校验

待决定：`lc submit` 在远程请求前是否编译整个 `solution.py`、提交 marker 区域，或完全交给 LeetCode 判定。该决定不得让 `lc submit` 隐式执行用户代码。

### PB-007：编辑器配置模型

待决定：命令参数、工作区配置、用户配置、环境变量和系统默认关联之间的优先级，以及编辑器配置使用单个命令字符串还是参数数组。配置设计必须避免通过 Shell 拼接执行命令。

### PB-010：分页越界

待决定：合法但超过题目总数的 `show --skip` 返回空结果、友好提示并成功结束，还是作为用户错误返回非零。非法负数和远端结构异常必须与合法空页区分。

### PB-011：Broken Pipe

待决定：`lc --help | head` 等下游提前关闭管道的场景是否视为正常结束，以及是否统一返回 0。

### PB-012：Python 支持范围

待决定：继续声明无上限的 `Python >=3.12`，还是只支持 CI 实际覆盖的小版本范围；同时明确 uv 是否允许自动使用 managed Python。

### PB-013：Git 工作区标记的版本控制语义

当前在 Git 仓库根目录执行 `lc init` 会创建 `.leetcode-local-cli.toml`，但项目不会自动修改 `.gitignore`。因此标记可能作为未跟踪文件出现在工作树中。

待决定：

1. 工作区标记是可提交的共享配置，由项目维护者决定是否纳入版本控制。
2. 工作区标记是纯本机配置，应由用户或模板明确忽略。
3. 调整标记位置或发现机制，使工作区识别不依赖仓库根目录中的未跟踪文件。

实现不得静默修改任意仓库的 `.gitignore`，除非这一外部副作用经过单独产品确认。

## 决策规则

每项待定边界拍板后，应完成以下同步：

1. 将结论、日期和适用版本写入本文件，并从“待定决策”移动到“已确认边界”。
2. 更新 README 的用户可见行为。
3. 更新 `DEVELOPMENT_PLAN.md` 的实施阶段与验收标准。
4. 如影响长期架构，更新 `PROJECT_DESIGN.md`。
5. 如影响安全或数据完整性，更新 `SECURITY_REVIEW.md`。
6. 实现代码与测试必须引用同一行为语义，不得只修改文档或只修改测试。

## 建议决策顺序

1. PB-002 扩展调试输出与 Doctor 执行信息。
2. PB-004 提交前本地校验。
3. PB-013 Git 工作区标记的版本控制语义。
4. PB-007 编辑器配置。
5. PB-010、PB-011、PB-012 的兼容性边界。
